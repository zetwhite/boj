#include <iostream> 
#include <vector> 
using namespace std; 
vector<int> ss; 
vector<int> fs; 
vector<int> all; 

int main(){
    int t; 
    cin >> t;
    int s, f;  
    fs.reserve(t); 
    ss.reserve(t); 
    all.reserve(2*t); 
    for(int i = 0; i < t; i++){
        cin >> s >> f; 
        
    }

    return 0; 
}